---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demoappservice
  labels:
    name: news
spec:
  replicas: 1
  selector:
    matchLabels:
      name: news
      app: news
  template:
    metadata:
      labels:
        name: news
        app: news
    spec:
      containers:
      - name: darth-vader
        image: {{ .Values.image.repositor.news }}
        env:
        - name: APIKEY
          value: ""
        ports:
        - name: news
          containerPort: 5000
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
      - name: obi-wan
        image: {{ .Values.image.repositor.fbi }}
        env:
        - name: APIKEY
          value: ""
        ports:
        - name: function
          containerPort: 5001
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
      - name: jarjar-binx
        image: {{ .Values.image.repositor.frauddet }}
        env:
        - name: APIKEY
          value: ""
        ports:
        - name: frauddet
          containerPort: 5002
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
      - name: luke-skywalker
        image: {{ .Values.image.repositor.function }}
        ports:
        - name: function
          containerPort: 5005
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"
          limits:
            memory: "128Mi"
            cpu: "500m"
