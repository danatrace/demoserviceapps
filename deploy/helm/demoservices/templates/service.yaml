apiVersion: v1
kind: Service
metadata:
  name: {{ include "demoservices.fullname" . }}
  labels:
    {{- include "demoservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demoservices.selectorLabels" . | nindent 4 }}
